---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: argocd-app
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: kazamisei98/nginx:v1.0
          ports:
            - containerPort: 80
